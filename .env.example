# Copy to .env (gitignored) and fill in. Never commit a real value, and never
# print one: every script reads the one key it needs and logs none of it.

# scripts/translate.mjs. Not needed for --dry-run.
DEEPSEEK_API_KEY=

# The forum scripts. Public reads need neither.
# TODO(iHiD): the rights this user needs are listed in orchestrator.md.
DISCOURSE_API_KEY=
DISCOURSE_API_USERNAME=
